// KEEP EFFECT TOKENS BELOW 32 CHARACTERS
void InitializeEffectTokens() {
    EFFECT_TOKENS.Init();

    EFFECT_TOKENS.SetPair("pure", 0);
    EFFECT_TOKENS.SetPair("lowgravity", 1);
    EFFECT_TOKENS.SetPair("medieval", 2);
    EFFECT_TOKENS.SetPair("crit", 3);
    EFFECT_TOKENS.SetPair("speedboost", 4);
    EFFECT_TOKENS.SetPair("thirdperson", 5);
    EFFECT_TOKENS.SetPair("vampire", 6);
    EFFECT_TOKENS.SetPair("swim", 7);
    EFFECT_TOKENS.SetPair("strongsuit", 8);
    EFFECT_TOKENS.SetPair("forcemelee", 9);
    EFFECT_TOKENS.SetPair("firemelee", 10);
    EFFECT_TOKENS.SetPair("schadenfreude", 11);
    EFFECT_TOKENS.SetPair("rngdeath", 12);
    EFFECT_TOKENS.SetPair("math", 13);
    EFFECT_TOKENS.SetPair("weaklings", 14);
    EFFECT_TOKENS.SetPair("bleedbuffed", 15);
    EFFECT_TOKENS.SetPair("rolemodel", 16);
    EFFECT_TOKENS.SetPair("classwars", 17);
    EFFECT_TOKENS.SetPair("snowball", 18);
    EFFECT_TOKENS.SetPair("justice", 19);
    EFFECT_TOKENS.SetPair("infection", 20);
    EFFECT_TOKENS.SetPair("duelies", 21);
    EFFECT_TOKENS.SetPair("heatwave", 22);
    EFFECT_TOKENS.SetPair("hyperheal", 23);
    EFFECT_TOKENS.SetPair("forceful", 24);
    EFFECT_TOKENS.SetPair("assassins", 25);
    EFFECT_TOKENS.SetPair("incontrol", 26);
    EFFECT_TOKENS.SetPair("bodycount", 27);
    EFFECT_TOKENS.SetPair("small", 28);
    EFFECT_TOKENS.SetPair("mosquito", 29);
    EFFECT_TOKENS.SetPair("balanced", 30);
    EFFECT_TOKENS.SetPair("pperform", 31);
    EFFECT_TOKENS.SetPair("slowmo", 32);
    EFFECT_TOKENS.SetPair("invis", 33);
    EFFECT_TOKENS.SetPair("secondary", 34);
    EFFECT_TOKENS.SetPair("hell", 35);
    EFFECT_TOKENS.SetPair("socialdist", 36);
    EFFECT_TOKENS.SetPair("disadvantaged", 37);
    EFFECT_TOKENS.SetPair("noscrubs", 38);
    EFFECT_TOKENS.SetPair("infiniteclip", 39);
    EFFECT_TOKENS.SetPair("restriction", 40);
    EFFECT_TOKENS.SetPair("nohud", 41);
    EFFECT_TOKENS.SetPair("horror", 42);
    EFFECT_TOKENS.SetPair("superjump", 43);
    EFFECT_TOKENS.SetPair("pulley", 44);
    EFFECT_TOKENS.SetPair("odglasscannon", 45);
    EFFECT_TOKENS.SetPair("buffersgambit", 46);
    EFFECT_TOKENS.SetPair("skatingrink", 47);
    EFFECT_TOKENS.SetPair("stunningmetal", 48);
    EFFECT_TOKENS.SetPair("deathstare", 49);
    EFFECT_TOKENS.SetPair("quickswap", 50);
    EFFECT_TOKENS.SetPair("mvm", 51);
    EFFECT_TOKENS.SetPair("pmayhem", 52);
    EFFECT_TOKENS.SetPair("shieldingmed", 53);
    EFFECT_TOKENS.SetPair("piercingbull", 54);
    EFFECT_TOKENS.SetPair("achievement", 55);
    EFFECT_TOKENS.SetPair("buffheal", 56);
    EFFECT_TOKENS.SetPair("mediccall", 57);
    EFFECT_TOKENS.SetPair("king", 58);
    EFFECT_TOKENS.SetPair("identitytheft", 59);
    EFFECT_TOKENS.SetPair("timetravel", 60);
    EFFECT_TOKENS.SetPair("parry", 61);
    EFFECT_TOKENS.SetPair("grapple", 62);
    EFFECT_TOKENS.SetPair("spellbound", 63);
}